---
id: Q15
rank: a6
labels:
    - docs
    - infra
status: ready
size: S
---

# Report the PreToolUse guard's own decisions in the activity report

The guard's denies and auto-allows sit in the same transcripts as `PreToolUse:Bash` attachments but go uncounted, so an over-fired `PR_SENTINEL_OVERRIDE` is invisible. Copy foreground-guard's shape.
