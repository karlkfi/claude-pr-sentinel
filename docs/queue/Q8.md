---
id: Q8
rank: a1
labels:
    - tests
    - retro
status: ready
size: S
---

# Require every `PR_SENTINEL_*` env var in the README

The README's Configuration table is the target. Same shape as the PRIVACY gate: extract `PR_SENTINEL_*` from the scripts and fail a var the README does not document. Passes today, so land a falsifiability test with it.
