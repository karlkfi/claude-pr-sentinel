---
id: Q4
rank: a7
labels:
    - watcher
status: deferred
size: S
---

# Distinguish required vs optional checks

**Event:** optional checks that genuinely fail their run wake sessions often (`continue-on-error` is already absorbed, #32). Then consult branch-protection required-check names.
